using UnityEngine;
using UnityEngine.UI;
using System.Collections;
using System;

public class ButtonChoose : MonoBehaviour
{


    [SerializeField]
    private Button btn1 = null; // assign in the editor
    private Button btn2 = null; // assign in the editor

    void Start()
    {
        //MyButton.onClick.AddListener(() => { Product(); });
    }

    // Update is called once per frame
    void Update()
    {

    }
}
